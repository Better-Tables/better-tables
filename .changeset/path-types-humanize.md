---
"@better-tables/core": minor
---

Finalize path-typed columns: document `Paths<Row, D>` depth override, wire
existing `humanize()` into bare `t.option().options([...])` auto-labels, and
align the experimental adapter facet signatures with `FacetQueryParams`.
