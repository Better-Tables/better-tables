---
"@better-tables/core": minor
---

Finalize path-typed columns: document `Paths<Row, D>` depth override, export
runtime `humanize()` and use it for bare `t.option().options([...])` auto-labels,
and align the experimental adapter facet signatures with `FacetQueryParams`.
