# Implementation Plans

Generated by the improve skill on 2026-07-12 (audit at commit `55dfd01`); maintained
through 2026-07-13 (baseline plans 012/013/001/004 and design plan 011 executed,
reviewed, and merged to local main). Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Selection note: the session was non-interactive, so per the skill's default the plans
cover the top findings by leverage **plus** the maintainer's explicitly stated goals
(Prisma/provider extension, AND/OR queries, full type safety, best-in-class DX).
Findings not planned this round are listed at the bottom so they aren't re-audited.

Verification baseline caveat: this checkout had no `node_modules` during the audit —
executors must run `bun install` first. The root `lint` script mutates files
(`biome check --write --unsafe .`); use `bunx biome check .` for check-only.

**RELEASE POLICY (maintainer directive, 2026-07-12)**: the next release is ONE
coordinated breaking upgrade — `0.6.0` (pre-1.0, so the 0.x minor is the
breaking slot; changesets for breaking changes use `minor`). Backward
compatibility is NOT required anywhere in this plan set: no deprecation
cycles, no compat overloads/shims, no parallel v2 methods. Remove replaced
surface outright; the deliverable owed to users is a MIGRATION GUIDE (one
follow-up plan should own writing it before the 0.6 publish). One deliberate
exception: URL wire-format READ compatibility (accepting old `c:` payloads)
is kept — shared/bookmarked URLs in the wild aren't API consumers, and the
fallback parse is near-zero cost. Plans written before this directive
contained softer language (deprecate-and-alias, parallel methods); where
found, it has been edited — if an executor encounters residual compat
language, this policy wins.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | CI gates every package (typecheck, lint, tests) | P1 | S | 012+013 (DONE) | DONE (2026-07-13: reviewer APPROVED, merged to local main — paths cover all packages, Bun pinned 1.3.1, static-checks job w/ non-blocking lint, test-cli job, test-summary gates all four. First real CI run happens when the remote is restored) |
| 002  | Stop mutations targeting the first schema table | P1 | M | — (001 rec.) | TODO |
| 003  | Fix join count inflation on MySQL/SQLite | P1 | S | — | TODO |
| 004  | Validate URL-deserialized state, fail closed | P1 | M | baseline (merged) | DONE (2026-07-13: reviewer APPROVED — `isFilterStateShape` boundary guard, fail-closed drops w/ value-free warnings, pagination clamping, 7 new tests (core 1007/0); merged to local main at `ce9f1ea`) |
| 005  | Builder type inference end-to-end | P1 | L | 001 (DONE) | PAUSED (2026-07-13: executor stopped by maintainer moments after dispatch, before any commits — re-dispatch fresh from current main when resuming; step 3 skipped per 011 decision) |
| 006  | Design: contract v2 — typed registry + AND/OR groups | P1 | L (design) | 005 | TODO |
| 007  | Extract ORM-agnostic adapter toolkit | P2 | L | 002, 003 (006 rec.) | TODO |
| 008  | Prisma adapter spike (read path) | P2 | L (spike) | 007 (hard), 006, 005 | TODO |
| 009  | DX hygiene sweep (README/packaging/CLI/onboarding) | P2 | M | — (001 rec.) | TODO |
| 010  | UI hooks correctness + first UI test harness | P2 | L | 001 | TODO |
| 011  | Design: Better-Auth-style config + path-typed table definitions | P1 | L (design) | — (co-evolves with 006; informs 005 step 3) | DONE (2026-07-12: reviewer APPROVED, merged to local main at `a82dd7a` on maintainer instruction. Perf gate: 199k instantiations / 1.03s, 10× under budget. Design content awaiting maintainer read — 5 open questions at the end of the doc. 005 step 3 skip-condition active pending maintainer approval of the design direction) |
| 012  | Repair verification baseline (lockfile sync, CLI/commander-14 fix) | P0 | M | — | DONE (2026-07-13: absorbed into 013's branch via cherry-pick; merged to local main at `e30ba00`) |
| 013  | Finish verification baseline (typecheck green, script unification, turbo, lint) | P0 | M–L | 012 commits (cherry-picked) | DONE (2026-07-13: reviewer APPROVED after re-running all gates — frozen install 0, root turbo typecheck 8/8, per-package typecheck 0×4, core 984/cli 127/drizzle-SQLite suites green, lint 162→71 errors; merged to local main at `e30ba00`. One REVISE round + two session-limit resumes along the way) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **The design spine (added 2026-07-12, maintainer brief)**: 011 designs the v1 developer-facing API — a Better-Auth-style `betterTables({ database: drizzle(db) | prisma(client), plugins })` config instance plus a path-typed column builder (`t.text('profile.location')` with dot-path autocomplete derived from the schema). It is design-only (doc + type prototype + perf fixture) and can start immediately. 006's typed registry must derive from 011's `define()` (see 006 Step 2 item 0); 005's step 3 (compile-gated `build()`) is SKIPPED if 011 is approved (see 005 Step 3 conditional); 008 validates 011's adapter `$types` protocol against real Prisma client types (008 Step 5 / findings item g).
- **005 → 006 → 008**: the typed column registry (006) requires literal-preserving builder ids (005); the Prisma spike (008) validates both the toolkit seams (007) and reads the 006 design for filter-group awareness.
- **002, 003 → 007**: land the known adapter bugs before the extraction refactor moves that code (007 deletes the `TODO(plan-007)` markers 002/003 leave).
- **001 first** where possible: it's the verification baseline; 010 assumes it and adds the UI test job.
- 004 and 009 are independent and can run any time.
- Suggested parallel tracks: (A) 001 → 004 → 005 → 006, (B) 002 → 003 → 007 → 008, (C) 009, 010, (D) 011 solo (design-only, touches only `plans/design/` + `experimental/` types) — tracks touch disjoint files except 010's small core addition (`FetchDataParams.signal`). Recommended: start 011 and 001 together; 011's approved design de-risks 005/006 before their heavy lifting begins.

## Findings audited but not planned this round

Correctness/quality — worth doing, lower leverage than the planned set:

- **CORE-02** Percentage formatter guesses decimal-vs-percent (`packages/core/src/lib/format-utils.ts:112-122`, `value > 1` heuristic) — wrong display for sub-1% values; needs an explicit config flag. S effort.
- **CORE-03** Dynamic row measurement leaves downstream offsets stale (`virtualization-manager.ts:485-495` recalculates only one row) — overlapping rows in dynamicRowHeight mode. M.
- **CORE-04** Date formatter appends a timezone label without converting and ignores configured locale (`lib/date-utils.ts:42-51`). M.
- **CORE-06** URL compression `renameKeys` recurses into user data; keys named `c/t/o/v/n/m/d` in filter meta/values get mangled on restore (`utils/compression.ts:56-72`). Fix together with 006's wire-format version bump (`c2:` prefix).
- **CORE-07** `updateState` emits several `state_changed` events per bulk update → refetch waterfall on URL hydration (`table-state-manager.ts:234-249`, `:522-577`). M; batching flag.
- **CORE-08** Six managers duplicate subscribe/notify with drifted error policy (silent swallow in `table-state-manager.ts:616-624` vs. `console.error` in `filter-manager.ts:489-498`). Extract an emitter base. M.
- **CORE-09** Virtualization offset math is O(n) per lookup (`virtualization-manager.ts:464-514`) — degrades past tens of thousands of rows; prefix-sum/Fenwick cache. L; needs 010's harness for regression tests first.
- **CORE-10** `includeNull`/`supportsNull` are dead in validation (`filter-manager.ts:360-441`) — null-only filters can't pass strict mode. Needs a semantics decision; fold into 006.
- **ADAPTER-03** Manual-join data queries paginate over row-multiplied results on one-to-many joins (flat `leftJoin` + `limit`, e.g. `postgres-query-builder.ts:503-539`; always on MySQL/SQLite) — pages under-fill even after 003 fixes `total`. L; needs distinct-PK subquery or relational routing. The biggest unplanned correctness item — plan it when 007 stabilizes the query-builder layer.
- **ADAPTER-05** Primary-table resolver `break`s on first relationship match and falls back to first table silently (`primary-table-resolver.ts:156-164`, `:222-225`); FK name-guessing can bind wrong columns (`relationship-detector.ts:1072-1088`). M; prefer explicit-config errors.
- **ADAPTER-06** Faceted values / min-max ignore active filters and inflate under joins (`drizzle-adapter.ts:744-788`). M; adapter contract may need widening — fold into 006/post-006.
- **ADAPTER-07** (partially covered by 004) Adapter-side silent filter drops on type mismatch (`filter-handler.ts:733-741`, per-operator `return undefined`) — fail-open. Ride along with 007's router extraction.
- **UI-05/06/08** Render-perf: unmemoized rows/cells in `table.tsx:788-887`; `VirtualizedTable` recreates per-row ResizeObservers every render (`virtualized-table.tsx:96-112`, `:196`); auto-show effect + six callback-bridge effects churn (`table.tsx:301-375`). M each; need 010's harness for safe memoization.
- **DX-10** Enable `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` — right for a type-safety-flagship library; blast radius unknown without a compile. Investigate after 005.
- **Follow-up from 009**: per-component subpath exports for `@better-tables/ui`.

Debt recorded by the 013 baseline run (2026-07-13):

- **apps/web excluded from typecheck** — 9 pre-existing errors (Next typegen globals `LayoutProps`/`PageProps`/`RouteContext` need its own `fumadocs-mdx && next typegen` pre-step, plus 2 stale `@ts-expect-error`). Re-include by wiring its `types:check` flow into the turbo task, not by deleting the record.
- **apps/marketing excluded from typecheck** — 62 pre-existing errors: it pins `@types/react@^18.3.27` against React-19-typed dependencies. This clashes with the React 19+ decision (see plan 009) — migrating marketing to React 19 types is the real fix and should ride with 009 or its own small plan.
- **Biome residue: 81 errors / 38 warnings / 3 infos as of the 001 merge** (162/76/3 pre-sweep → 71/38/1 post-013 → +10 errors/+2 infos from plan 011's never-linted `experimental/` + type-fixture files, which merged separately; those need biome-ignore pragmas or fixes when the prototype is promoted out of `experimental/`). Original post-sweep breakdown: By rule: 25 noNonNullAssertion, 21 noSvgWithoutTitle, 11 noArrayIndexKey, 6 noConsole, 5 noExplicitAny, 4 useButtonType, 3 noDangerouslySetInnerHtml, 3 noBannedTypes, 2 noImgElement, 2 noUnusedFunctionParameters, 3 misc — plus 24 parse/format diagnostics from biome's CSS parser vs Tailwind v4 at-rules in `apps/{demo,marketing}` globals.css (tooling limitation, not code). CI lint starts `continue-on-error` per plan 001 Step 4c until this reaches 0.

Considered and rejected:

- Package `exports` maps ordering — checked; `types` condition already first, ESM/CJS paired correctly. No action.
- `FilterState` discriminated-union design — well-modeled; only the `columnId` linkage gap (planned via 006). No structural change needed.
- Core resource leaks — none found; the one `ResizeObserver` is disconnected properly; table-registry lifecycle is GC-safe.
- Type escape hatches in core — notably clean (zero `as any`/`@ts-ignore` in `packages/core/src`); the two `biome-ignore noExplicitAny` sites are justified. The `any` that matters was the ui boundary (planned in 005).
- JSONB/array `sql.raw` paths in the Drizzle adapter — audited; adequately defended (anchored regex allowlists, parameterized field binding). Only the identifier-quoting seam was weak (folded into 007 step 3).
- CSV export injection — already handled (formula-prefix escaping + quote doubling, `drizzle-adapter.ts:1316-1323`). No action.
